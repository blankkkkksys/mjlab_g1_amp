Copy trained AMP policy here before building/running g1_ctrl:

  logs/rsl_rl/g1_amp_locomotion/<run>/policy.onnx
  logs/rsl_rl/g1_amp_locomotion/<run>/policy.onnx.data   (if present)

Target files:
  policy.onnx
  policy.onnx.data
